/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import React from "react";

const H50: React.FC = () => (
  <div
    css={css({
      height: 50,
    })}
  />
);

export default H50;
