export interface Council {
  name: string;
  link: string;
  year: number;
  slogan: string;
  thumbnail: string;
  people: string;
  description: string;
}

export interface CouncilEvent {
  id: number;
  title: string;
  description: string;
}